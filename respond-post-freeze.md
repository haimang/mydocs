## {N}. 补强 / 更新章节（post-freeze addendum，{DATE} — {一句话主题}）

> **用法（respond- 附加章节，append-only）**：
> 1. **宿主**：任何已 `frozen` 的 `design` / 设计型 eval（偶尔 action-plan）。
> 2. **挂载位置**：**续宿主既有编号**（`## {N}`，接在最后一节之后）。**不重编号、不插入、不改写冻结主体**。
> 3. **谁写 / 何时**：由**原作者**在主体 `frozen` 之后、因 **owner 裁决 / 驳回 / 提问** 触发时追加。
> 4. **append-only（最硬纪律）**：**不重开任何冻结裁决**。冻结的*决策*不动；冻结的*措辞 / 理由*可校准（须注明"不改裁决"）；**要 supersede 某条冻结结论，必须点名，并回上游 QnA register 重开对应 Q，不在别处悄悄改口**。
> 5. **回链 item-ID**：新工作项一律用**新 ID 前缀**（如 `R-*` 区别于原 `P*/F*`）；缺口 / 价值表对照**当前代码**复核，而非旧水位 / 旧态。
> 6. **取证**：本节新增的每个 `file:line` 均须**本轮 verify**。
> 7. **多轮**：每次补强追加一个 dated `## {N}` 章；在宿主「版本历史」表 append 一行（**冻结行不动**）。

> 性质：本节是 §0–§`{FROZEN_N}`（frozen `{VER}`）的事后补强附录；不重开冻结。
> 触发：`{OWNER_RULING_OR_REJECTION_OR_QUESTION_原文}`
> 方法 & 证据：新增 file:line 均经本轮 verify（见 §{N}.1）。
> （可选）先认错：`{owner ruling 是否成立 / 接受批评}`

### {N}.0 判据 / 约束重述（如适用；校准而非改写冻结理由）

| # | 判据 | 含义 |
|---|------|------|
| `C1` | `{CRITERION}` | `{MEANING}` |
| `C2` | `{CRITERION}` | `{MEANING}` |

### {N}.1 重新基线化的缺口 / 价值台账（对照当前代码，{DATE} 复核）

| 旧标缺口 / item | 当前真实状态（file:line） | 状态 | 本节处理 |
|------------------|----------------------------|------|----------|
| `{ITEM}` | `{REAL_STATE}` | `✅ 已交付 \| 🟩 剩余在 wave-N \| 🟥 缩水/丢失 \| ⬜ OUT` | `{HANDLING}` |

### {N}.2 补强 / re-scope 工作列表（新 ID 前缀，区别于原 P*/F*）

| 编号 | 工作项 | 宿主锚点（file:line） | @layer | 净新增 | 价值 | 风险 |
|------|--------|------------------------|--------|--------|------|------|
| `R-A1` | `{ITEM}` | `{ANCHOR}` | `logic \| protocol \| infra` | `{NET_NEW}` | `高/中/低` | `低/中/高` |

显式 defer 子组：

| 编号 | defer 去向 | 理由 |
|------|-----------|------|
| `R-C1` | `{SUCCESSOR_DOC_OR_PHASE}` | `{WHY}` |

### {N}.3 收口标准 / 自检矩阵

| 补强项 | C1 | C2 | … |
|--------|----|----|---|
| `R-A1` | `✅` | `⚠️` | `…` |

### {N}.4 待议 / 已裁决开放项

- ~~`{OPEN_ITEM}`~~ → 裁决：`{DECISION}`
- `{STILL_OPEN_ITEM}` → 待 `{WHO}` 裁决

### {N}.5 诚实边界 / supersede 声明

- **被本节超越的冻结口径**：超越冻结 §`{X}` 的 `{CLAUSE}`；如要正式推翻，须回 `{UPSTREAM_QNA}` 重开 `{Q-ID}`，**不在本节或别处改口**。
- **取证**：`{EVIDENCE_FILE_LINES}`。
- **不做什么**：本节不重开 §`{FROZEN_N}` 任何裁决；被驳回的下游产物维持其状态。

> **多轮回填**：在宿主「版本历史」表追加 `| {NEW_VER} | {DATE} | {AUTHOR} | append §{N}（{主题}） |`（冻结行保持不动）。

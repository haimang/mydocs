# Planning 簇 · proposed —— 收窄重锚（三态成熟链 · 站②）

> **模板血统**：`planning`（独立高频血统：[[planning-initial]] → `planning-proposed`（本文）→ [[planning-final]]。取代旧 `eval-planning.md`[已 retired]）。
> **一句话用途**：拿 reference-anchor 回灌的事实，把 [[planning-initial]] 的 first-cut **逐项辨证收窄、重锚到 HEAD `file:line`**——把核查到的真相记入真相台账（`T-R`），据真相重调 DAG/AP 并溯因，作 **pre-charter-qna 前唯一精炼工作基线**，交 [[planning-final]] critique。
> **何时用**：[[planning-initial]] 已产出、reference-anchor 已回灌、本链进入"收窄重锚"成熟态。**何时不用**：见 [[planning-initial]] 头部。
>
> **承重梁（planning 簇共有脊 · 三态一致 · 不可删）**：
> 1. **逐级辨证 + 调整溯因（§3 承重段）**：§3 = **Δ 表裁定 [[planning-initial]]**（`KEEP/REFRAME/CLOSED/NEW`），且每项调整**绑定驱动它的 Truth-ID**（§2 真相台账）。**§3 缺失 = 链断**。
> 2. **双门渐进冻结 + 真相台账（§2 · Module①）**：proposed 是 **reference-checked 真相主要析出态**。§2 真相台账**继承** initial 的 `T-O`（foundational）+ **新增 `T-R`**（reference-anchor + HEAD 实测事实），每条 `T-R` 标它**证实/证伪了** initial 哪条 `T-P`。
> 3. **planning 文档冻结零决策（精确义）**：真相由 reference-anchor / HEAD / qna 冻结，本文件只**带类型 CITE**（引 `RA-n / file:line / Q编号`），不自冻。
> 4. **逐级 supersede 不回改前序**：本文取代 initial；真相台账**跨态 append-only**（Truth-ID 稳定）。
> 5. **族 → 链 → 态**：本文件是阶段族 `{FAMILY}` 下 `{CHAIN_NAME}` 链的**站②**；一族可多链。
> 6. **锚点纪律**：proposed 起，每工作项须有 HEAD `file:line` 锚 + reference verdict + TR 过滤（剔除非 substrate 机制）。
>
> **状态词汇**：`draft | reviewed | superseded`。**命名提示**：`proposed-planning[-on-<scope>][-by-<author>].md`；**N→1 合流**实例可命名 `proposed-planning-on-unified-<path>.md`。
> **占位符约定**：`{UPPER_SNAKE}` backtick 包裹；`[核心]` 必填 / `[可选]` 视删。

---

# `{SUBPHASE}` —— proposed-planning（planning · 站② proposed）by `{AUTHOR}`

> **stage**：`proposed`（站②·收窄/重锚）
> **作者**：`{AUTHOR}`（panel / 跨模型 handoff：`{PANEL_OR_NONE}`——proposed 常由不同模型写，下一态继承其骨架）· **时间**：`{DATE}`
> **本链 scope-fence**：阶段族 `{FAMILY}` 下 `{CHAIN_NAME}` 链站②。
> **文档性质（自宣告）**：**取代 [[planning-initial]]**，作 pre-charter-qna 前**唯一精炼工作基线**；本文件冻结零决策（仅 CITE 真相）。
> **上游权威输入**：`{initial-planning（或多份，见 §3）/ reference-anchor / HEAD 实测}`
> **下游消费者**：[[planning-final]]（站③）+ `{PHASE}/pre-charter-qna.md`（owner-gate 裁决）
> **文档状态**：`draft | reviewed | superseded`

---

## 0. TL;DR `[核心]`

- **核心论点**：`{ONE_PARAGRAPH_THESIS}`
- **本态相对 initial 做了什么（一句话 supersession）**：`{CORE_REFRAME，如"从'激活'改为'D1 引擎重建'，← T-R-3 实测"}`
- **本态新增 reference-checked 真相**：`{N 条 T-R——证实 X 条暂定前提、证伪 Y 条}`

---

## 1. Reference anchors / 输入与依据 `[核心]`

> proposed 的翻新由**新证据**驱动：reference-anchor / ARCH / TR 过滤。

| 输入 | 类型 | 提供了什么 | 锚点 |
|------|------|------------|------|
| `{INPUT}` | `initial-plan / reference-anchor / HEAD / qna` | `{WHAT}` | `{FILE_OR_LINE / RA-n}` |

---

## 2. 规划真相台账（Planning Truth Register · 站②）★ `[核心]`

> **Module① · 本态真相切片**。继承 initial 的 `T-O`（foundational）+ **新增 `T-R`**（reference-checked）。CITE 不自冻。

### 2.1 继承（截至 initial）
> 引 [[planning-initial]] §2 的 `T-O`（foundational）；本态据其约束。`{列 Truth-ID 一行摘要 / 或"见 initial §2"}`

### 2.2 新增 reference-checked 真相（本态析出）★

| Truth-ID | 类型 | 真相内容（一句话）| 来源（`file:line` / RA-n）| 证实/证伪了哪条暂定前提 | 触发的 AP/DAG 调整 |
|----------|------|--------------------|-----------------------------|--------------------------|---------------------|
| `T-R-1` | `reference-checked` | `{如：KV 1/s→429+无 read-after-write，不能做权威热层}` | `{file:line}` | `证伪 T-P-2` | `{GTF3-02 REFRAME → substrate 改}` |

---

## 3. 辨证审核（Δ 裁定上一阶段）+ 调整溯因 ★ 承重段 `[核心]`

> **站②承重梁**。裁定动词（默认，可覆盖）：`KEEP / REFRAME / CLOSED / NEW`。**每行须带"驱动真相 Truth-ID"列**（Module②：该调整受哪条真相约束）。本站须能表达三种实测前序形态：
> - **① 标准**（裁定一份 initial）；
> - **② 多前序合流（N→1）**：同时取代**多份**并行 initial，一张 Δ 表同时裁定，交集重复地基抽成净新合并项（`NEW`）；在 §3.0 列被合流前序；
> - **③ 前序替换**：本链无 `initial-planning`，proposed 直接裁更重的上游（如 `initial-investigation-index`）；§3 抬头注明裁定对象。

- **§3.0 裁定对象（前序清单）**：`{被本态 supersede 的一份或多份前序 + 各自 scope}`
- **§3.1 Δ 表 + 调整溯因**：

| item-ID | 裁定（KEEP/REFRAME/CLOSED/NEW）| 来源前序 | 重分配 phase | 复用（✅/♻️/🆕）| **驱动真相（Truth-ID）** | 理由 / 新证据 |
|---------|-------------------------------|----------|--------------|------|--------------------------|------|
| `{X3-01}` | `{VERDICT}` | `{initial-A / initial-B}` | `{PHASE}` | `{REUSE}` | `{T-R-1 / T-O-2}` | `{WHY}` |

- **本态核心转向（一句话）**：`{CORE_REFRAME}`（← `{驱动 Truth-ID}`）

### 3.C [可选 · 外审横向插入] 外部 review 裁定
> 收到外部 reviewed-by-`<model>`：在此就地 trust-code 逐条裁定其 findings，本文件版本号自增（如 v0.4）吸收；review 揭示的新事实**升格为 `T-R` 入 §2**。

| review item | 裁定（ADOPT / ADOPT-substance / REJECT）| 处置 | 依据 / 升格 Truth-ID |
|-------------|-----------------------------------------|------|----------------------|
| `{R1}` | `{VERDICT}` | `{HANDLING}` | `{ANCHOR / T-R-?}` |

---

## 4. 范围与非范围（In/Out-Scope · sized 仍 gated）`[核心]`

- **In-Scope [S1]**：`{ITEM}`（已 sized，仍 gated；受 `{T-?}`）
- **Out-of-Scope / 延后 [O1]**：`{ITEM}`；重评条件：`{REVISIT}`

---

## 5. 跨阶段贯穿主题（threaded themes）`[核心]`

- **技术路线红线**：`{TR_RED_LINES}` · **治理冻结面**：`{…}` · **migration inventory**：`{036/037/...}`

---

## 6. DAG（关键路径 + 并行窗）+ DAG 调整溯因 `[核心]`

```text
{CRITICAL_PATH + 并行窗}
```

### 6.1 DAG 调整溯因（Module② · 相对 initial DAG 改了什么 ← 哪条真相）
| DAG 变更 | 相对 initial | **驱动真相（Truth-ID）** | 说明 |
|----------|--------------|--------------------------|------|
| `{如：GTF5 前移为硬前置}` | `{原并行 → 现串行}` | `{T-R-4 / T-O-1}` | `{WHY}` |

---

## 7. 逐 phase 工作台账 —— 重分配 + verdict 绑定 + 拆解 `[核心]`

> 每工作项绑 reference 轴 + ✅蓝本 + HEAD 锚(`file:line`) + ⛔避坑 + TR；净新/高风险**拆 a/b/c**；migrations 编号。ID 跨态稳定。

### 7.x `{PHASE_NAME}`

| 编号 | 工作项 | reference 轴 + ✅蓝本 + HEAD 锚(`file:line`) + ⛔避坑 + TR | 复用 | 规模 | 受约束真相 |
|------|--------|----------------------------------------------------------|------|------|------------|
| `{X4-03a}` | `{ITEM}` | `{ANCHORS}` | `✅/♻️/🆕` | `{SIZE}` | `{T-?}` |

---

## 8. Owner decision gates —— 精炼 OPEN `[核心]`

| 编号 | 决策点 | 影响 | 依新 sizing 精炼的建议 | 状态 |
|------|--------|------|------------------------|------|
| `G-X-1` | `{DECISION}` | `{IMPACT}` | `{RECOMMENDATION}` | `OPEN` |

---

## 9. 测试计划（+ DoD）`[核心]`

- **A 短途 / B spike / D mega**：`{SCOPE}` · **DoD（每 phase 收口判据雏形）**：`{DOD}`

---

## 10. 风险登记 `[核心]`

| 风险 | 触发 | 影响 | 缓解 |
|------|------|------|------|
| `{RISK}` | `{TRIGGER}` | `{IMPACT}` | `{MITIGATION}` |

---

## 11. 后继解锁 `[核心]`

- **解锁的下游价值**：`{DOWNSTREAM_VALUE}`
- **下游**：→ [[planning-final]]（站③，将 critique 本文并冻结派生 action-plan）

---

## 12. 交叉引用与修订历史 `[可选]`

- **交叉引用**：上游 [[planning-initial]] · 下游 [[planning-final]]
- **修订历史**（跨模型 proposed 可缺）：

| 版本 | 日期 | 作者 | 主要变更 |
|------|------|------|----------|
| v0.1 | `{DATE}` | `{AUTHOR}` | 初稿（Δ 裁定 + 重锚 + 真相台账[+T-R] + 调整溯因）|

---

## 附录 · 站② 速用（本态填哪些）

| 段 | 站② proposed 填法 |
|----|------------------|
| §2 真相台账 | 继承 `T-O` + 新增 `T-R`（证实/证伪暂定前提）|
| §3 辨证审核 | Δ 表 vs initial（支持 N→1 合流 / 前序替换）+ **驱动真相列** + 3.C 外审 |
| §6 DAG | + 6.1 DAG 调整溯因（← Truth-ID）|
| §7 工作台账 | 重分配 + verdict 绑定 + a/b/c 拆解 + HEAD 锚 |
| 自宣告 role | "取代 initial，唯一精炼工作基线" |
